ANTHROPIC_API_KEY=your-anthropic-api-key
ACTIVE_BRAND=cprime

# TEMPORARY POC ACCESS CONTROL — replace with proper auth/SSO before production use.
# Generate with: openssl rand -hex 32
# Leave unset or empty to disable protection during local development.
APP_ACCESS_SECRET=replace-with-a-long-random-secret
APP_CONTACT_EMAIL=hello@shophouseinc.com

# BetterStack — leave empty to disable logging locally.
BETTERSTACK_SOURCE_TOKEN=your-betterstack-source-token
