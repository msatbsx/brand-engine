node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results

#claude
.claude

# Brand document source PDFs — too large for git (>100MB).
# Run `yarn preprocess` locally to regenerate the *-extracted.md files.
src/lib/brand-documents/*.pdf
